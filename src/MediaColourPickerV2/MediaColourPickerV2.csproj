<Project Sdk="Microsoft.NET.Sdk.Razor">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <StaticWebAssetBasePath>/</StaticWebAssetBasePath>
  </PropertyGroup>

  <PropertyGroup>
    <Product>Umbraco.Community.MediaColourPickerV2</Product>
    <PackageId>Umbraco.Community.MediaColourPickerV2</PackageId>
    <Title>Media Colour Picker V2</Title>
    <Description>
      With this Umbraco CMS package, you can now add a new custom property editor to your uploaded images. This feature enables you to use the focal point of an image to obtain its brightest color, average color, and opposite color to the brightest.
    </Description>
    <PackageTags>umbraco;umbraco-marketplace</PackageTags>
    <Authors>Owain Williams</Authors>
    <Copyright>$([System.DateTime]::UtcNow.ToString(`yyyy`)) © Owain Williams</Copyright>
    <PackageProjectUrl>https://github.com/OwainWilliams/MediaColourPickerV2</PackageProjectUrl>
    <RepositoryUrl>https://github.com/OwainWilliams/MediaColourPickerV2</RepositoryUrl>
    <PackageReadmeFile>README_nuget.md</PackageReadmeFile>
    <RepositoryType>git</RepositoryType>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageIcon></PackageIcon>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Umbraco.Cms.Web.Website" Version="17.0.0-rc2" />
    <PackageReference Include="Umbraco.Cms.Web.Common" Version="17.0.0-rc2" />
    <PackageReference Include="Umbraco.Cms.Api.Common" Version="17.0.0-rc2" />
    <PackageReference Include="Umbraco.Cms.Api.Management" Version="17.0.0-rc2" />
  </ItemGroup>

  <ItemGroup>
    <!-- Dont include the client folder as part of packaging nuget build -->
    <Content Remove="Client\**" />

    <!-- However make the Umbraco-package.json included for dotnet pack or nuget package and visible to the solution -->
    <None Include="Client\public\umbraco-package.json" Pack="false" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\..\docs\README_nuget.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <Folder Include="Controllers\" />
  </ItemGroup>

</Project>